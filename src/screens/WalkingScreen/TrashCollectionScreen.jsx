//쓰레기봉투 인식 카메라
import React, { useState, useRef } from 'react';
import { View, StyleSheet } from 'react-native';


const TrashCollectionScreen = () => {
  
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default TrashCollectionScreen;